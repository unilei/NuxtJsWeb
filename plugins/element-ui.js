import Vue from 'vue'
import Element from 'element-ui'
import locale from 'element-ui/lib/locale/lang/en'

Vue.use(Element, { locale })
// 全局引用
// import Element from 'element-ui'

// 按需引用
// import {Header,Main,Aside,Footer,Message,Container,Upload } from 'element-ui'

// Vue.use(Element, { locale })

// 按需使用
// Vue.use(Header, { locale })
// Vue.use(Main, { locale })
// Vue.use(Aside, { locale })
// Vue.use(Footer, { locale })
// Vue.use(Message, { locale })
// Vue.use(Container, { locale })
// Vue.use(Upload, { locale })
//
// Vue.prototype.$message = Message;
// Vue.prototype.$ELEMENT = { size: 'small', zIndex: 3000 };
